langcode: en
status: true
dependencies:
  config:
    - user.role.repository_administrator
  module:
    - user
id: user_add_role_action.repository_administrator
label: 'Add the Repository Administrator role to the selected user(s)'
type: user
plugin: user_add_role_action
configuration:
  rid: repository_administrator
